meta {
  name: Call API with OAuth Token
  type: http
  seq: 13
}

get {
  url: {{base_url}}/api/movies/list
  body: none
}

headers {
  Authorization: Bearer {{access_token}}
}

docs {
  This calls your API using the access token obtained from the OAuth flow.
  Replace '/api/whatever' with the actual endpoint you want to test.
}